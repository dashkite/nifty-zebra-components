:host
  display block

article
  display flex
  flex-flow column
  row-gap 2rem
  max-width 48rem

  & > section
    display flex
    flex-wrap wrap
    gap 2rem

    section
      display flex
      flex-flow column
      gap 1rem
      flex 1

    section:nth-child(3)
      flex-basis 100%
    label
      font-weight bold
